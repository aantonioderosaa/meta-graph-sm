# Event-triage loop progress

Source plan: `../piano-implementativo-giudice-eventi-assert-retract.md`
Theory refs: `../1.metagraph-formalizzazione-rev.md`, `../2.metagraph-kernel-tre-assi (2).md`, `../3.metagraph-pipeline-formalizzazione (1).md`, `../4.metagraph-specifiche-aperte-baseline.md`, `../5.metagraph-layer-agentico-contesto.md`
Allowed write root: `sm/` only.
Git remote: `https://github.com/aantonioderosaa/meta-graph-sm.git` (repo is `sm/`).
Loop sentinel: `AGENT_LOOP_WAKE_eventtriage`

## Current

- **Completed macrotask:** 5 — awaiting parent commit
- **Next macrotask:** 6 — Stato "in attesa" (`:PendingEventContext` listen-window)
- **Status:** complete (awaiting parent commit)
- **Do not start:** Macrotask 6+ until 5 is committed and pushed.

## Macrotasks

| MT | Nome | Status |
|---|---|---|
| 0 | Flag e schema additivo | completed |
| 1 | Slot model e OR-Set testimoni | completed |
| 2 | Estensione LWW per gli attributi | completed |
| 3 | Provenienza e versionamento | completed |
| 4 | Validazione kernel-safe | completed |
| 5 | `_task_event_triage` nel giudice | completed |
| 6 | Stato "in attesa" (`:PendingEventContext`) | pending |
| 7 | Endpoint e pannello incompletezze | pending |
| 8 | Test di non regressione a corpus fisso | pending (write in parallel; green only after 7) |

## Notes

- After each macrotask: verify acceptance criteria, commit, push to origin, update README if needed.
- Do not modify files outside `sm/`.
- `ENABLE_EVENT_TRIAGE` stays False until Macrotask 8 is green in CI.
- Never DELETE, never merge_nodes, never touch identity.
- Quantifier (F21.1) and retraction (F21.2) stay unchanged.
- Kernel Famiglia B stays closed — no new relation type.

## Macrotask 0 notes

### Files touched

- `sm/backend/app/core/config.py` — `ENABLE_EVENT_TRIAGE: bool = False` after `ENABLE_CONTEXT_LAYER` (independent field, default False).
- `sm/backend/app/db/schema.cypher` — comment-only block documenting schema-optional `:Relation.caused_by_event_id` / `run_id` and `:Node.revisions`. No CREATE CONSTRAINT / INDEX.
- `sm/backend/app/db/schema.py` — comment documenting the same free properties; not added to `REQUIRED_*`.
- `sm/.env.example` — `ENABLE_EVENT_TRIAGE=false` with short comment.
- `sm/docker-compose.yml` — backend env `ENABLE_EVENT_TRIAGE: ${ENABLE_EVENT_TRIAGE:-false}`.
- `sm/README.md` — Documentazione bullet, rollout-policy sentence (flag stays False until Macrotask 8 is green), Flag Metagraph table row.
- `sm/backend/tests/test_acceptance_migration.py` — `assert fields["ENABLE_EVENT_TRIAGE"].default is False`.
- `sm/.event-triage-loop-progress.md` — this file.

Not wired into `judge.py` / `dreaming.py` (Macrotask 5). No `event_slots.py`. Famiglia B unchanged.

### Tests run

From `sm/backend` with `.venv\Scripts\python.exe`:

```
pytest tests/test_acceptance_migration.py tests/test_schema.py::test_schema_file_has_expected_statements tests/test_acceptance_judge.py tests/test_dreaming_nodes.py tests/test_acceptance_context_agent.py -q --tb=short
```

**41 passed** in 7.27s.

`ruff check` on `app/core/config.py`, `app/db/schema.py`, `tests/test_acceptance_migration.py`: **All checks passed.**

`EXPECTED_SCHEMA_STATEMENTS` remains **27**.

### Deviations

None.

### Leftover gaps

None.

## Macrotask 1 notes

### Files touched

- `sm/backend/app/pipeline/event_slots.py` — new module: `slot_id` / `Slot` / `assert_slot` / `retract_slot`, witness OR-Set on `witness_source_ids` (+ `witness_add_tags`), `slot_id` stamped on `:Relation`.
- `sm/backend/tests/test_event_slots.py` — FakeSession dispatching on Cypher constants; zero Neo4j/OpenAI.
- `sm/.event-triage-loop-progress.md` — this file.

Not modified: `ingestion.py` (`write_node_relation` reused as-is; slot properties SET after CREATE), `reconcile.py`, `quantifier_events.py`, `retraction.py`, `kernel.py`, `judge.py`, `dreaming.py`, `config.py`. Flag stays False. Famiglia B unchanged.

### Tests run

From `sm/backend` with `.venv\Scripts\python.exe`:

```
pytest tests/test_event_slots.py tests/test_acceptance_migration.py tests/test_acceptance_judge.py tests/test_acceptance_retraction.py -q --tb=short
```

**36 passed** in 5.92s (`test_event_slots.py`: 12 collected).

`ruff check` on `app/pipeline/event_slots.py`, `tests/test_event_slots.py`: **All checks passed.**

### Deviations

- Did not extend `CREATE_NODE_RELATION_CYPHER`; after `write_node_relation`, a module-constant `SET` stamps `slot_id` / witness id lists. Avoids churn in Fase 0–22 tests that compare that Cypher string.
- OR-Set add-tags live in `witness_add_tags` (`fonte_id:uuid`); the counted supporting set is still unique `witness_source_ids`. Duplicate assert of an already-supporting fonte is a no-op (no second id).
- `witness_target_ids` is kept empty unless already present; provenance `fonte_id` is the source-side supporting set.
- Relation retract may `write_contradicts` only when another `is_latest` fact on the same `(head, kernel_parent)` remains. No new Famiglia B type.

### Leftover gaps

- Attribute LWW across different tails (both can stay `is_latest=true`) is Macrotask 2 — `reconcile_scoped_attribute_slots` not called.
- `caused_by_event_id` / `run_id` not written (Macrotask 3).
- `validate_slot_proposal` not implemented (Macrotask 4).
- Not wired into `judge.py` / dreaming; `ENABLE_EVENT_TRIAGE` remains False.

## Macrotask 2 notes

### Files touched

- `sm/backend/app/pipeline/reconcile.py` — new `reconcile_scoped_attribute_slots`: groups AttributeKernelType edges by `slot_id` (else `(head_id, kernel_parent)`). Newest `is_latest=true`, older `is_latest=false`, additive `updates` property on the newer edge pointing at the previous tail. `reconcile_scoped_relations` and `RECONCILE_SCOPED_RELATIONS_CYPHER` unchanged.
- `sm/backend/app/pipeline/event_slots.py` — after an attribute `assert_slot` that created/touched a slot, calls `reconcile_scoped_attribute_slots` scoped to `(head_id, slot_id)`; no-op when only one latest edge.
- `sm/backend/tests/test_event_slots.py` — FakeSession dispatch for the new Cypher; leftover two-tail test now expects LWW.
- `sm/backend/tests/test_reconcile_attribute_slots.py` — new FakeSession tests (LWW, relation slots not collapsed, Fase path does not import the new function).
- `sm/.event-triage-loop-progress.md` — this file.

Not modified: `dreaming.py`, `ingestion.py`, `judge.py`, `entity_relation_resolution.py`, `quantifier_events.py`, `retraction.py`, `kernel.py`, `config.py`. Flag stays False. Famiglia B unchanged. No DELETE.

### Tests run

From `sm/backend` with `.venv\Scripts\python.exe`:

```
pytest tests/test_event_slots.py tests/test_reconcile_relations.py tests/test_reconcile_attribute_slots.py tests/test_acceptance_judge.py tests/test_acceptance_temporal_axis.py -q --tb=short
```

**49 passed** in 5.80s (`test_event_slots.py`: 12, `test_reconcile_relations.py`: 3, `test_reconcile_attribute_slots.py`: 9, `test_acceptance_judge.py`: 12, `test_acceptance_temporal_axis.py`: 13).

`ruff check` on `app/pipeline/reconcile.py`, `app/pipeline/event_slots.py`, `tests/test_event_slots.py`, `tests/test_reconcile_attribute_slots.py`: **All checks passed.**

### Deviations

- Wired from `assert_slot` (allowed; plan said "mai chiamata dal path Fase 0-18/19-22 esistente" — `assert_slot` is not on that path). Makes the "dopo riconciliazione" AC automatic.
- LWW uses an additive `updates` property on the newer `:Relation` (previous tail id), not `normalized_relation='updates'`, so `kernel_parent` / attribute type stay intact.
- Implemented as FIND + SET Cypher constants with Python grouping (FakeSession-testable) rather than one Neo4j `WITH a, b` query. Existing `(head,tail)` function stays bit-identical.

### Leftover gaps

- `caused_by_event_id` / `run_id` not written (Macrotask 3) — done in MT3.
- `validate_slot_proposal` not implemented (Macrotask 4).
- Not wired into `judge.py` / dreaming; `ENABLE_EVENT_TRIAGE` remains False.
- `reconcile_scoped_relations` still cannot see two attribute values (different tails) — by design; that path is unchanged.

## Macrotask 3 notes

### Files touched

- `sm/backend/app/pipeline/event_slots.py` — `assert_slot` / `retract_slot` require non-empty `caused_by_event_id` and `run_id`; STAMP and UPDATE SET both on every written/updated `:Relation`. New `append_node_revision` (read current scalar, append `{property, old_value, event_id, run_id, at}` to `revisions`, then SET new value).
- `sm/backend/tests/test_event_slots.py` — FakeSession dispatch for provenance + revision Cypher; call sites pass the new kwargs; provenance and empty-id tests.
- `sm/backend/tests/test_reconcile_attribute_slots.py` — `assert_slot` call sites pass provenance kwargs.
- `sm/backend/tests/test_node_revisions.py` — two successive `summary` writes; first `old_value` remains; empty event/run raises.
- `sm/.event-triage-loop-progress.md` — this file.

Not modified: `ingestion.py` (`CREATE_NODE_RELATION_CYPHER` unchanged), `reconcile.py`, `judge.py`, `dreaming.py`, `quantifier_events.py`, `retraction.py`, `kernel.py`, `config.py`, schema. Flag stays False. No DELETE, no index/constraint. Famiglia B unchanged.

### Tests run

From `sm/backend` with `.venv\Scripts\python.exe`:

```
pytest tests/test_event_slots.py tests/test_reconcile_attribute_slots.py tests/test_acceptance_judge.py tests/test_node_revisions.py -q --tb=short
```

**40 passed** in 6.34s (`test_event_slots.py`: 15, `test_reconcile_attribute_slots.py`: 9, `test_acceptance_judge.py`: 12, `test_node_revisions.py`: 4).

`ruff check` on `app/pipeline/event_slots.py`, `tests/test_event_slots.py`, `tests/test_reconcile_attribute_slots.py`, `tests/test_node_revisions.py`: **All checks passed.**

### Deviations

- `append_node_revision` takes `new_value` and reads the pre-write scalar itself (allowed variant) rather than trusting a caller-supplied `old_value`.
- Refuses identity properties (`id`, `merged_into`) so this path cannot touch identity.
- Does not stamp `:CONTRADICTS` from `write_contradicts` (not a `:Relation`; only STAMP/UPDATE slot edges get provenance).
- Not called from ingest/dream/judge; tests call it directly (MT5 may use it when an assert also updates `summary`).

### Leftover gaps

- `validate_slot_proposal` not implemented (Macrotask 4).
- Not wired into `judge.py` / dreaming; `ENABLE_EVENT_TRIAGE` remains False.
- `append_node_revision` is not yet invoked from `assert_slot` when a summary (or other scalar) also changes.

## Macrotask 4 notes

### Files touched

- `sm/backend/app/pipeline/event_slots.py` — `validate_slot_proposal` → `ValidatedSlot | None`; exact `RelationKernelType` / `AttributeKernelType` member only (enum or value string). `apply_validated_slot` writes only after validation (`None` / invented kernel → zero Cypher). Does not call `assert_slot`/`retract_slot` from the validator itself.
- `sm/backend/tests/test_validate_slot_proposal.py` — FakeSession; invented kernel zero writes; exact `"Stato"` / `"Causale"` apply; malformed input never raises.
- `sm/.event-triage-loop-progress.md` — this file.

Not modified: `kernel.py` (Famiglia B closed), `judge.py`, `dreaming.py`, `config.py`, ingestion/reconcile/quantifier/retraction. Flag stays False. No DELETE, no merge_nodes, no identity.

### Tests run

From `sm/backend` with `.venv\Scripts\python.exe`:

```
pytest tests/test_validate_slot_proposal.py tests/test_event_slots.py tests/test_node_revisions.py tests/test_kernel_constants.py -q --tb=short
```

**69 passed** in 5.90s (`test_validate_slot_proposal.py`: 42, `test_event_slots.py`: 15, `test_node_revisions.py`: 4, `test_kernel_constants.py`: 8).

`ruff check` on `app/pipeline/event_slots.py`, `tests/test_validate_slot_proposal.py`: **All checks passed.**

### Deviations

- Validator lives in `event_slots.py` (not a sibling module) so MT5 has one import surface.
- `apply_validated_slot` also accepts a raw proposal: it validates first, then writes. `None` / invalid → no session calls.
- Attribute `retract` may omit `tail_id` (attribute slot identity ignores tail). `assert` and all relation slots still require `tail_id`.
- Aliases accepted: `head`/`head_id`, `fonte`/`fonte_id`, `tail`/`tail_id`, `verb`/`verbo`, `relation`/`normalized_relation`. Missing relation defaults to the kernel value — not a kernel coerce.
- `ValidatedSlot.kernel_parent` is the enum member; `slot.kernel_parent` is the exact value string.

### Leftover gaps

- Not wired into `judge.py` / dreaming (Macrotask 5). `ENABLE_EVENT_TRIAGE` remains False.
- No `EventSlotProposal` structured schema yet (MT5 `call_structured`).
- `:EventTriageRun` / `:PendingEventContext` not created (MT5–6).
- `append_node_revision` still not invoked from `assert_slot` when a scalar also changes.

## Macrotask 5 notes

### Files touched

- `sm/backend/app/pipeline/event_triage.py` — new module: `EventSlotProposal` / `run_event_triage`, one `call_structured` per event, fan-out cap `EVENT_TRIAGE_MAX_SLOT_FANOUT=8`, audit `MERGE_EVENT_TRIAGE_RUN_CYPHER`, thin `MERGE_PENDING_EVENT_CONTEXT_CYPHER` for `waiting`. Slot writes only via `validate_slot_proposal` / `apply_validated_slot`. LLM error on one event → `waiting`, continue. Never raises.
- `sm/backend/app/pipeline/judge.py` — `_task_event_triage` thin wrapper (no Cypher literals), last task after `_task_temporal`, gated on `ENABLE_EVENT_TRIAGE`.
- `sm/backend/app/db/schema.cypher` — comment-only `:EventTriageRun` / `:PendingEventContext` (no constraint). `EXPECTED_SCHEMA_STATEMENTS` stays 27.
- `sm/backend/app/db/schema.py` — comment documenting the same.
- `sm/README.md` — event-triage bullet: `_task_event_triage` exists, gated, default off.
- `sm/backend/tests/test_event_triage.py` — FakeSession tests (flag off, idempotent confirmed, invented kernel, LLM error isolation, no ad-hoc mutating Cypher).
- `sm/.event-triage-loop-progress.md` — this file.

Not modified: `quantifier_events.py`, `retraction.py`, `kernel.py`, `config.py` (flag stays False), `.env.example`, `docker-compose.yml`, `event_slots.py`. No DELETE, no merge_nodes, no identity. Famiglia B unchanged.

### Tests run

From `sm/backend` with `.venv\Scripts\python.exe`:

```
pytest tests/test_event_triage.py tests/test_acceptance_judge.py tests/test_validate_slot_proposal.py tests/test_schema.py::test_schema_file_has_expected_statements tests/test_acceptance_migration.py -q --tb=short
```

**65 passed** in 6.28s (`test_event_triage.py`: 5, `test_acceptance_judge.py`: 12, `test_validate_slot_proposal.py`: 42, `test_schema.py::test_schema_file_has_expected_statements`: 1, `test_acceptance_migration.py`: 5).

`ruff check` on `app/pipeline/event_triage.py`, `app/pipeline/judge.py`, `app/db/schema.py`, `tests/test_event_triage.py`: **All checks passed.**

### Deviations

- Body lives in `event_triage.py`; `_task_event_triage` is a try/except wrapper with no Cypher (plan AC).
- One `call_structured` per event (YAGNI vs ReAct). Read-only `get_relations` / `get_metadata` prepended into the prompt; LLM cannot emit Cypher.
- Thin `:PendingEventContext` MERGE on `waiting` (queryable; no `checks_without_progress` / listen-window expiry — MT6). Waiting events from that record are retried before new batch events.
- Fan-out is module constant `EVENT_TRIAGE_MAX_SLOT_FANOUT=8`, not a new Settings flag.
- `incomplete` is never written here (MT6 window).

### Leftover gaps

- Listen-window expiry to `incomplete` (Macrotask 6).
- Incompleteness API / UI (Macrotask 7).
- Three corpus e2e cases (Macrotask 8).
- `ENABLE_EVENT_TRIAGE` remains False.
- `append_node_revision` still not invoked from `assert_slot` when a scalar also changes.
- Optional `simple_fact` already-present confirmation path not implemented (plan listed it as optional).


