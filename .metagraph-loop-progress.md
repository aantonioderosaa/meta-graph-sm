# Metagraph loop progress

Source plan: `../piano-implementativo-metagraph.md`
Allowed write root: `sm/` only.
Git remote: `https://github.com/aantonioderosaa/meta-graph-sm.git` (repo is `sm/`).

## Current

- **Completed phase:** 8 — Identità per faccette
- **Next phase:** 9 — Asse temporale
- **Status:** Fase 8 accepted; stop before Fase 9 (temporal axis). `ENABLE_FACET_IDENTITY` default **false**; `merge_nodes` kept. Facet path is non-destructive (no move/delete of `:Relation` / `HAS_CONCEPT` / `DERIVED_FROM`, no `merged_into`).

## Phases

| Fase | Nome | Status |
|---|---|---|
| 0 | Fondamenta del kernel | completed |
| 1 | Book del dominio come regole operative | completed |
| 2 | Modello dati Neo4j esteso | completed |
| 3 | Ingestione anti-blur e conforme al kernel | completed |
| 4 | Backbone/TBox e classificazione delle entità | completed |
| 5 | PROMOTE e ricorsione dei sottodomini | completed |
| 6 | Popolamento con i fatti: LCA ed eventi collante | completed |
| 7 | Relazioni nei quattro modi e strati S0/S1/S2 | completed |
| 8 | Identità per faccette | completed |
| 9 | Asse temporale | pending |
| 10 | Il giudice | pending |
| 11 | Query engine: coarse-to-fine, S0/S2 | pending |
| 12 | Frontend | pending |
| 13 | Migrazione e coesistenza | pending |
| 14 | Qualità e accettazione end-to-end | pending |
| 15 | Frontend — vista a grafo generale | pending |

## Fase 0 notes

- Vocabulary declared in `backend/app/models/kernel.py` (E1–E8 / A1–A7 / R1–R6 / Famiglia B + backbone). Zero pipeline hooks.
- `pytest tests/test_kernel_constants.py -q --tb=short`: 8 passed.
- `ruff check` on new/touched model files: all checks passed.
- No leftover gaps.

## Fase 1 notes

- Book + gates in `backend/app/pipeline/domain_book.py` (8 `CATEGORY_CARDS`, `passes_genre_vs_filter_gate`, `passes_mdl_gate`, `GENRE_NOT_TOPIC_PROMPT`). Settings `BACKBONE_MDL_MIN_COVERAGE=5` / `BACKBONE_MDL_MIN_PAYLOAD=2`. Zero pipeline hooks.
- `pytest tests/test_domain_book.py tests/test_kernel_constants.py -q --tb=short`: 19 passed.
- `ruff check` on new/touched files: all checks passed.
- No leftover gaps.

## Fase 2 notes

- Additive schema in `backend/app/db/schema.cypher` (23 statements, all `IF NOT EXISTS`). New constraints: `identity_node_uri`, `connectivity_rule_triple`, `corpus_context_id`. New btree indexes: `concept_kernel_category`, `concept_parent_uri`, `node_kernel_category`. No new vector indexes. `FAMIGLIA_B_REL_TYPES` / `BACKBONE_REL_TYPES` locked to kernel enums. Zero pipeline writes.
- `pytest tests/test_schema.py::test_schema_file_has_expected_statements` + kernel/book: 20 passed.
- `pytest tests/test_schema.py` (Docker, apply twice): 3 passed.
- `ruff check` on touched schema files: all checks passed.
- No leftover gaps.

## Fase 3 notes

- Two-pass ingest in existing modules: Pass A `extract_entities` (name + summary + `kernel_category` E1–E8), Pass B one LLM call per unordered pair (`PairRelationDecision`). Entity–entity `:Relation` only if `related` + both witnesses. F3.0 `:CorpusContext {id:"default"}` O(1) update before chunking. `pipeline_complete` still last SSE event.
- Witnesses required on `EntityRelationTriple` / `EventRelationTriple` (`Field(min_length=1)`). Pre-write filter `has_required_witnesses`. Same-chunk conflicts write both facts + `:CONTRADICTS` (no `RelationLabel` change, dreaming untouched).
- `pytest tests/test_acceptance_ingest_antiblur.py tests/test_node_extraction.py tests/test_node_extraction_prompt.py tests/test_kernel_constants.py tests/test_domain_book.py tests/test_chunking.py tests/test_acceptance_nodes.py -q --tb=short`: 51 passed.
- Docker: `test_ingestion_integration.py` (3) + `test_graph_reset.py` (2) + `test_nodes_integration.py` (3) passed.
- `ruff check` on touched files: all checks passed.
- Deviations: local refinement frontier omitted (R1–R6 in prompts suffice); within-chunk node reuse by `(type, normalized name)` so pair endpoints are stable; `kernel_category` written on `:Node` in Pass A (F4.1 will consume it). No Fase 4 backbone/`MEMBER_OF`.

## Fase 4 notes

- Two-level Concept match in `backend/app/pipeline/concepts.py` (exact id/name then `concept_embedding` + `kernel_category`). Unique home is `MEMBER_OF`; `HAS_CONCEPT` unchanged (`merge_concept_and_link`). Stage `classify_and_grow_backbone` in `backend/app/pipeline/backbone.py`, after entity resolution in dreaming. Catch-all vertices `compute_hash_id("kernel:{Category}")`. New named genre only if `passes_genre_vs_filter_gate`; MDL reserved for Fase 5 `PROMOTE`. `ENABLE_KERNEL_CLASSIFICATION` default True. Kill switch no-op. `:UnanchoredCandidate` for gate fail / missing `kernel_category`.
- Decision: `score >= 0.80` reuse; `[0.50, 0.80)` catch-all; no match / `< 0.50` propose (gate pass → new genre `IS_A` catch-all; gate fail → catch-all + UnanchoredCandidate). Second call with existing `MEMBER_OF` is a no-op.
- `pytest tests/test_acceptance_backbone.py tests/test_dreaming_nodes.py tests/test_kernel_constants.py tests/test_domain_book.py tests/test_acceptance_ingest_antiblur.py tests/test_node_extraction.py tests/test_concepts.py -q --tb=short`: 72 passed.
- `ruff check` on touched files: all checks passed.
- Deviations: no schema.cypher index for UnanchoredCandidate; MDL/`Cluster` not called in F4 (would fail k=5 on singletons; documented). No `promote.py`. Frontend PipelineMonitor gained `backbone_classification` so SSE is visible.
- No leftover gaps. Next = Fase 5.

## Fase 5 notes

- `backend/app/pipeline/promote.py`: `promote(session, parent, ids) -> str` with gates **before** any write (membership, same `kernel_category`, genre-vs-filter, MDL k∧m). Writes in one `execute_write` txn: CREATE S, `IS_A` parent, move `MEMBER_OF`, CREATE-lift externals (witnesses append leaf id, no MERGE, leaf facts kept), MERGE identity μ on `:TypeMigrationAlias`. Deterministic id `compute_hash_id("promote:"+parent+":"+sorted ids)`; existing S is an immediate no-op.
- F5.8: parent must be a kernel catch-all or `IS_A` a kernel catch-all; clusters of `:Concept` are skipped. `promote_clusters` snapshots kernel + first-level catch-alls and promotes all direct `:Node` members as one cluster. Stage wired in dreaming after backbone / before relation+event. `ENABLE_PROMOTE` default True.
- `pytest tests/test_acceptance_promote.py tests/test_acceptance_backbone.py tests/test_dreaming_nodes.py tests/test_domain_book.py tests/test_concepts.py -q --tb=short`: 63 passed.
- `ruff check` on new/touched files: all checks passed.
- Deviations: identity μ (`new_type = old_type`) — no richer rename yet; no `schema.cypher` constraint for `:TypeMigrationAlias`; LLM `definition` skipped when `OPENAI_API_KEY` is empty (empty string is acceptable); optional `definition_kind` kwarg so filter clusters can skip without extra LLM clustering. Frontend PipelineMonitor gained `promote_clusters`.
- One-level limit is explicit. Next = Fase 6.

## Fase 6 notes

- `backend/app/pipeline/lca.py`: `compute_lca` climbs unique `MEMBER_OF` then `IS_A` (home-first); `lca_from_ancestor_lists` is the pure helper. `facts_visible_in_subdomain` / `witnessed_neighbors` are read-only traversals of leaf `:Relation` (both ends `:Node`, `lifted_from IS NULL`). No CREATE at the parent Concept. Cypher lives here and is re-exported from `node_graph_engine.py`.
- F6.3: `reify_shared_situation` in `event_relation_resolution.py` MERGE/reuses `:Node {type:'event', kernel_category:'Evento'}` plus R5 `participates` (`is participated by`). No entity–entity context edge. Helper + tests only (ingestion already writes leaf facts once).
- `pytest tests/test_acceptance_fact_placement.py tests/test_node_graph_engine.py tests/test_acceptance_promote.py tests/test_event_relation_resolution.py tests/test_domain_book.py -q --tb=short`: 65 passed.
- `ruff check` on new/touched files: all checks passed.
- Deviations: no whole-graph auto-scan and no ingestion hook (not trivial). PROMOTE Concept-endpoint shadows (`lifted_from`) are filtered, not undone. No `:ConnectivityRule` / S2.
- Next = Fase 7.

## Fase 7 notes

- S0: `write_node_relation` refuses blank `kernel_parent` (no CREATE). After a successful CREATE it calls `deposit_from_asserted_fact` (unique `:ConnectivityRule` channel). `update_bundle()` in `promote.py` CREATE-lifts distinct externals in the same `execute_write` txn (no MERGE/fusion).
- S1: `connectivity_rules.py` MERGE on `(source_category, relation_type, target_category)`; `origin_fact_ids` grows on MATCH. Generalization walks `IS_A` up to `CONNECTIVITY_MAX_GENERALIZATION_HOPS=1` and stops before kernel catch-all ids. No `kernel:Agente —rel→ kernel:Agente`. Nodes with only `kernel_category` deposit level 0 only.
- S2: `derive_candidate_links` in `node_query_engine.py` returns `DerivedLink` + `DerivationStep` chain in memory. Requires an S1 type match **and** a leaf S0 descent path (length 2–3 or shared-neighbor 2-hop); no path → discard, not “uncertain”. Zero CREATE/MERGE of `:Relation`.
- `pytest tests/test_acceptance_s0_s1_s2.py tests/test_connectivity_rules.py tests/test_acceptance_ingest_antiblur.py tests/test_acceptance_promote.py tests/test_node_query_embeddings.py tests/test_node_extraction.py tests/test_node_query_engine.py -q --tb=short`: 64 passed.
- `ruff check` on new/touched files: all checks passed.
- Deviations: no deposit hook from `reify_shared_situation` (optional; those writes use their own MERGE Cypher). Fase 11 `epistemic_status` on QueryAnswer not added (dedicated dataclass from derive). No identity facets.
- S0/S2 wall noted. Next = Fase 8.

## Fase 8 notes

- `backend/app/pipeline/identity_resolution.py`: `ensure_identity_node`, `link_as_facet` (`(:Node)-[:SAME_AS]->(:IdentityNode)`), `unlink_facet` (DELETE SAME_AS/POSSIBLY_SAME_AS on that pair only), `link_possibly_same_as`, `mark_not_same_as`. URI: `identity:{normalized_name}:{kernel_category}` (fallback `identity:{sorted_facet_ids}`). Creating identity does not destroy facets.
- Blocking (pure): same `kernel_category` AND (normalized name / alias / cosine ≥ `IDENTITY_BLOCK_THRESHOLD=0.82`). Different category never a candidate. `NOT_SAME_AS` skipped. `resolve_node` with flag on writes POSSIBLY_SAME_AS and returns the facet id; flag off still `merge_nodes`. Dreaming does not call `link_as_facet` (judge-only SAME_AS, Fase 10).
- `pytest tests/test_acceptance_identity_facets.py tests/test_node_resolution.py tests/test_dreaming_nodes.py tests/test_acceptance_backbone.py -q --tb=short`: 51 passed.
- `ruff check` on new/touched files: all checks passed.
- Deviations: `NOT_SAME_AS` is a dedicated Neo4j rel type, not added to frozen `SpecialRelationType` (kernel stays 7 members). Graph load uses `coalesce(n.summary_embedding, n.embedding)` because `:Node` currently stores `embedding`. No IdentityNode attached on POSSIBLY_SAME_AS (not required).
- Next = Fase 9. Do not start the temporal axis in this phase.

## Notes

- After each phase: verify acceptance criteria, commit, push to origin, update README if needed.
- Do not modify files outside `sm/`.
