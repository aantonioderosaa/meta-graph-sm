# Metagraph loop progress

Source plan: `../piano-implementativo-metagraph.md`
Allowed write root: `sm/` only.
Git remote: `https://github.com/aantonioderosaa/meta-graph-sm.git` (repo is `sm/`).

## Current

- **Completed phase:** 21 — Quantificatori come evento e ritrattazioni globali
- **Next phase:** 22 — Orchestrazione: il flusso agentico di verifica. Do **not** start Fase 22 (`context_agent.py` / `:AgentSearchRun`) or Fase 23 in this loop.
- **Status:** Fase 21 **completed**. Fase 22 planned, not started. Fase 23 is scope-only (not decomposed into tasks) — do not start it. Only `sm/` may be modified.

## Phases

| Fase | Nome | Status |
|---|---|---|
| 0 | Fondamenta del kernel | completed |
| 1 | Book del dominio come regole operative | completed |
| 2 | Modello dati Neo4j esteso | completed |
| 3 | Ingestione anti-blur e conforme al kernel | completed |
| 4 | Backbone/TBox e classificazione delle entità | completed |
| 5 | PROMOTE e ricorsione dei sottodomini | completed |
| 6 | Popolamento con i fatti: LCA ed eventi collante | completed |
| 7 | Relazioni nei quattro modi e strati S0/S1/S2 | completed |
| 8 | Identità per faccette | completed |
| 9 | Asse temporale | completed |
| 10 | Il giudice | completed |
| 11 | Query engine: coarse-to-fine, S0/S2 | completed |
| 12 | Frontend | completed |
| 13 | Migrazione e coesistenza | completed |
| 14 | Qualità e accettazione end-to-end | completed |
| 15 | Frontend — vista a grafo generale | completed |
| 17 | Dashboard sottodomini trasparente | completed |
| 18 | Riconciliazione incrementale su ingestioni successive | completed |
| 19 | Retrieval con metadati (prerequisito del layer agentico) | completed |
| 20 | Filtro di rilevanza strutturale e ipotesi con confidenza | completed |
| 21 | Quantificatori come evento e ritrattazioni globali | completed |
| 22 | Orchestrazione: il flusso agentico di verifica | open |
| 23 | Istanze generiche per sottodominio | scope only, not planned |

## Fase 0 notes

- Vocabulary declared in `backend/app/models/kernel.py` (E1–E8 / A1–A7 / R1–R6 / Famiglia B + backbone). Zero pipeline hooks.
- `pytest tests/test_kernel_constants.py -q --tb=short`: 8 passed.
- `ruff check` on new/touched model files: all checks passed.
- No leftover gaps.

## Fase 1 notes

- Book + gates in `backend/app/pipeline/domain_book.py` (8 `CATEGORY_CARDS`, `passes_genre_vs_filter_gate`, `passes_mdl_gate`, `GENRE_NOT_TOPIC_PROMPT`). Settings `BACKBONE_MDL_MIN_COVERAGE=5` / `BACKBONE_MDL_MIN_PAYLOAD=2`. Zero pipeline hooks.
- `pytest tests/test_domain_book.py tests/test_kernel_constants.py -q --tb=short`: 19 passed.
- `ruff check` on new/touched files: all checks passed.
- No leftover gaps.

## Fase 2 notes

- Additive schema in `backend/app/db/schema.cypher` (23 statements, all `IF NOT EXISTS`). New constraints: `identity_node_uri`, `connectivity_rule_triple`, `corpus_context_id`. New btree indexes: `concept_kernel_category`, `concept_parent_uri`, `node_kernel_category`. No new vector indexes. `FAMIGLIA_B_REL_TYPES` / `BACKBONE_REL_TYPES` locked to kernel enums. Zero pipeline writes.
- `pytest tests/test_schema.py::test_schema_file_has_expected_statements` + kernel/book: 20 passed.
- `pytest tests/test_schema.py` (Docker, apply twice): 3 passed.
- `ruff check` on touched schema files: all checks passed.
- No leftover gaps.

## Fase 3 notes

- Two-pass ingest in existing modules: Pass A `extract_entities` (name + summary + `kernel_category` E1–E8), Pass B one LLM call per unordered pair (`PairRelationDecision`). Entity–entity `:Relation` only if `related` + both witnesses. F3.0 `:CorpusContext {id:"default"}` O(1) update before chunking. `pipeline_complete` still last SSE event.
- Witnesses required on `EntityRelationTriple` / `EventRelationTriple` (`Field(min_length=1)`). Pre-write filter `has_required_witnesses`. Same-chunk conflicts write both facts + `:CONTRADICTS` (no `RelationLabel` change, dreaming untouched).
- `pytest tests/test_acceptance_ingest_antiblur.py tests/test_node_extraction.py tests/test_node_extraction_prompt.py tests/test_kernel_constants.py tests/test_domain_book.py tests/test_chunking.py tests/test_acceptance_nodes.py -q --tb=short`: 51 passed.
- Docker: `test_ingestion_integration.py` (3) + `test_graph_reset.py` (2) + `test_nodes_integration.py` (3) passed.
- `ruff check` on touched files: all checks passed.
- Deviations: local refinement frontier omitted (R1–R6 in prompts suffice); within-chunk node reuse by `(type, normalized name)` so pair endpoints are stable; `kernel_category` written on `:Node` in Pass A (F4.1 will consume it). No Fase 4 backbone/`MEMBER_OF`.

## Fase 4 notes

- Two-level Concept match in `backend/app/pipeline/concepts.py` (exact id/name then `concept_embedding` + `kernel_category`). Unique home is `MEMBER_OF`; `HAS_CONCEPT` unchanged (`merge_concept_and_link`). Stage `classify_and_grow_backbone` in `backend/app/pipeline/backbone.py`, after entity resolution in dreaming. Catch-all vertices `compute_hash_id("kernel:{Category}")`. New named genre only if `passes_genre_vs_filter_gate`; MDL reserved for Fase 5 `PROMOTE`. `ENABLE_KERNEL_CLASSIFICATION` default True. Kill switch no-op. `:UnanchoredCandidate` for gate fail / missing `kernel_category`.
- Decision: `score >= 0.80` reuse; `[0.50, 0.80)` catch-all; no match / `< 0.50` propose (gate pass → new genre `IS_A` catch-all; gate fail → catch-all + UnanchoredCandidate). Second call with existing `MEMBER_OF` is a no-op.
- `pytest tests/test_acceptance_backbone.py tests/test_dreaming_nodes.py tests/test_kernel_constants.py tests/test_domain_book.py tests/test_acceptance_ingest_antiblur.py tests/test_node_extraction.py tests/test_concepts.py -q --tb=short`: 72 passed.
- `ruff check` on touched files: all checks passed.
- Deviations: no schema.cypher index for UnanchoredCandidate; MDL/`Cluster` not called in F4 (would fail k=5 on singletons; documented). No `promote.py`. Frontend PipelineMonitor gained `backbone_classification` so SSE is visible.
- No leftover gaps. Next = Fase 5.

## Fase 5 notes

- `backend/app/pipeline/promote.py`: `promote(session, parent, ids) -> str` with gates **before** any write (membership, same `kernel_category`, genre-vs-filter, MDL k∧m). Writes in one `execute_write` txn: CREATE S, `IS_A` parent, move `MEMBER_OF`, CREATE-lift externals (witnesses append leaf id, no MERGE, leaf facts kept), MERGE identity μ on `:TypeMigrationAlias`. Deterministic id `compute_hash_id("promote:"+parent+":"+sorted ids)`; existing S is an immediate no-op.
- F5.8: parent must be a kernel catch-all or `IS_A` a kernel catch-all; clusters of `:Concept` are skipped. `promote_clusters` snapshots kernel + first-level catch-alls and promotes all direct `:Node` members as one cluster. Stage wired in dreaming after backbone / before relation+event. `ENABLE_PROMOTE` default True.
- `pytest tests/test_acceptance_promote.py tests/test_acceptance_backbone.py tests/test_dreaming_nodes.py tests/test_domain_book.py tests/test_concepts.py -q --tb=short`: 63 passed.
- `ruff check` on new/touched files: all checks passed.
- Deviations: identity μ (`new_type = old_type`) — no richer rename yet; no `schema.cypher` constraint for `:TypeMigrationAlias`; LLM `definition` skipped when `OPENAI_API_KEY` is empty (empty string is acceptable); optional `definition_kind` kwarg so filter clusters can skip without extra LLM clustering. Frontend PipelineMonitor gained `promote_clusters`.
- One-level limit is explicit. Next = Fase 6.

## Fase 6 notes

- `backend/app/pipeline/lca.py`: `compute_lca` climbs unique `MEMBER_OF` then `IS_A` (home-first); `lca_from_ancestor_lists` is the pure helper. `facts_visible_in_subdomain` / `witnessed_neighbors` are read-only traversals of leaf `:Relation` (both ends `:Node`, `lifted_from IS NULL`). No CREATE at the parent Concept. Cypher lives here and is re-exported from `node_graph_engine.py`.
- F6.3: `reify_shared_situation` in `event_relation_resolution.py` MERGE/reuses `:Node {type:'event', kernel_category:'Evento'}` plus R5 `participates` (`is participated by`). No entity–entity context edge. Helper + tests only (ingestion already writes leaf facts once).
- `pytest tests/test_acceptance_fact_placement.py tests/test_node_graph_engine.py tests/test_acceptance_promote.py tests/test_event_relation_resolution.py tests/test_domain_book.py -q --tb=short`: 65 passed.
- `ruff check` on new/touched files: all checks passed.
- Deviations: no whole-graph auto-scan and no ingestion hook (not trivial). PROMOTE Concept-endpoint shadows (`lifted_from`) are filtered, not undone. No `:ConnectivityRule` / S2.
- Next = Fase 7.

## Fase 7 notes

- S0: `write_node_relation` refuses blank `kernel_parent` (no CREATE). After a successful CREATE it calls `deposit_from_asserted_fact` (unique `:ConnectivityRule` channel). `update_bundle()` in `promote.py` CREATE-lifts distinct externals in the same `execute_write` txn (no MERGE/fusion).
- S1: `connectivity_rules.py` MERGE on `(source_category, relation_type, target_category)`; `origin_fact_ids` grows on MATCH. Generalization walks `IS_A` up to `CONNECTIVITY_MAX_GENERALIZATION_HOPS=1` and stops before kernel catch-all ids. No `kernel:Agente —rel→ kernel:Agente`. Nodes with only `kernel_category` deposit level 0 only.
- S2: `derive_candidate_links` in `node_query_engine.py` returns `DerivedLink` + `DerivationStep` chain in memory. Requires an S1 type match **and** a leaf S0 descent path (length 2–3 or shared-neighbor 2-hop); no path → discard, not “uncertain”. Zero CREATE/MERGE of `:Relation`.
- `pytest tests/test_acceptance_s0_s1_s2.py tests/test_connectivity_rules.py tests/test_acceptance_ingest_antiblur.py tests/test_acceptance_promote.py tests/test_node_query_embeddings.py tests/test_node_extraction.py tests/test_node_query_engine.py -q --tb=short`: 64 passed.
- `ruff check` on new/touched files: all checks passed.
- Deviations: no deposit hook from `reify_shared_situation` (optional; those writes use their own MERGE Cypher). Fase 11 `epistemic_status` on QueryAnswer not added (dedicated dataclass from derive). No identity facets.
- S0/S2 wall noted. Next = Fase 8.

## Fase 8 notes

- `backend/app/pipeline/identity_resolution.py`: `ensure_identity_node`, `link_as_facet` (`(:Node)-[:SAME_AS]->(:IdentityNode)`), `unlink_facet` (DELETE SAME_AS/POSSIBLY_SAME_AS on that pair only), `link_possibly_same_as`, `mark_not_same_as`. URI: `identity:{normalized_name}:{kernel_category}` (fallback `identity:{sorted_facet_ids}`). Creating identity does not destroy facets.
- Blocking (pure): same `kernel_category` AND (normalized name / alias / cosine ≥ `IDENTITY_BLOCK_THRESHOLD=0.82`). Different category never a candidate. `NOT_SAME_AS` skipped. `resolve_node` with flag on writes POSSIBLY_SAME_AS and returns the facet id; flag off still `merge_nodes`. Dreaming does not call `link_as_facet` (judge-only SAME_AS, Fase 10).
- `pytest tests/test_acceptance_identity_facets.py tests/test_node_resolution.py tests/test_dreaming_nodes.py tests/test_acceptance_backbone.py -q --tb=short`: 51 passed.
- `ruff check` on new/touched files: all checks passed.
- Deviations: `NOT_SAME_AS` is a dedicated Neo4j rel type, not added to frozen `SpecialRelationType` (kernel stays 7 members). Graph load uses `coalesce(n.summary_embedding, n.embedding)` because `:Node` currently stores `embedding`. No IdentityNode attached on POSSIBLY_SAME_AS (not required).
- Next = Fase 9. Do not start the temporal axis in this phase.

## Fase 9 notes

- T1 extended in `entity_relation_resolution.py`: `RelationLabel` gains `supersedes` / `updated_by` / `contradicts` (`replaces` kept for the flag-off path). Default prompt is three-way. `ENABLE_TEMPORAL_TRANSITIONS` default true; OR with `ENABLE_FACET_IDENTITY`. Both false → `APPLY_UPDATES_CYPHER` for `replaces`.
- Apply: SUPERSEDES/UPDATED_BY set old `is_latest=false`, keep the old `:Relation`, CREATE Famiglia B between assertion tails with `{subject_id, new_rel_id, old_rel_id}`, `normalized_relation='updates'` as compat alias. CONTRADICTS reuses `write_contradicts`; both stay latest. `extends` unchanged.
- F9.1: `index_entity_version` MERGEs SAME_AS to `:IdentityNode` (no edge move). F9.4: `write_node_relation(valid_time=..., provenance=...)`; `system_time: datetime()` stays a separate property.
- `pytest tests/test_acceptance_temporal_axis.py tests/test_relation_prompt.py tests/test_entity_relation_resolution.py tests/test_models.py tests/test_acceptance_identity_facets.py -q --tb=short`: 58 passed.
- `ruff check` on touched files: all checks passed.
- Deviations: same-endpoint SUPERSEDES/UPDATED_BY is a Famiglia B self-loop on the tail (T1 candidates share head+tail; F3.5 CONTRADICTS is tail-to-tail). No judge (Fase 10). No dreaming rewrite for version indexing.
- Next = Fase 10. Do not start the judge in this phase.

## Fase 10 notes

- `backend/app/pipeline/judge.py`: `run_judge` after `reconcile` in `run_dreaming_pipeline` (before PPR). Six tasks: anti-blur (clear multi-witness lists + `requeue_pair` callback, no S2 facts), `EQUIVALENT_TO` collapse at `BACKBONE_COLLAPSE_THRESHOLD=0.90` with `MEMBER_OF` move + `absorbed_from`, historical re-refine scoped to `promoted_parent_ids` from `promote_clusters(..., parent_ids_out=)`, identity confirm via `link_as_facet` / `mark_not_same_as`, missed `CONTRADICTS` via `write_contradicts`, temporal re-sort to `SUPERSEDES`/`UPDATED_BY` without deleting assertions. Always MERGE `:JudgeRun`. Exceptions wrapped so `pipeline_complete` still fires. `ENABLE_JUDGE` default True.
- SSE `judge_complete` + PipelineMonitor stage `judge`. Kill switch still logs `:JudgeRun` with zeros.
- `pytest tests/test_acceptance_judge.py tests/test_dreaming_nodes.py tests/test_acceptance_identity_facets.py tests/test_acceptance_temporal_axis.py -q --tb=short`: 52 passed.
- `ruff check` on new/touched files: all checks passed.
- Deviations: production anti-blur does not re-invoke `entity_relation_resolution` in the same batch (`requeue_pair` is a no-op; tests inject a callback). Temporal reclass CREATE is tail-to-tail Famiglia B (same as F3.5 CONTRADICTS), not `APPLY_SUPERSEDES_CYPHER` (would flip `is_latest`). No new kernel types.
- Next = Fase 11. Do not start query `epistemic_status` in this phase.

## Fase 11 notes

- F11.1: `QueryCitation` + Pydantic `DerivationStep` on `NodeQueryResponse.citations` (default `[]`). Validator: `derived` requires a non-empty chain. Engine dataclass removed — one shape, converted at the API boundary by construction. Labels computed in Python (`label_query_citations`), not by the LLM.
- F11.2: `plan_connectivity_scope` infers E1–E8 via token/synonym match (Persona→Agente, Organizzazione→CostruttoSociale, …), queries `:ConnectivityRule`, then `hybrid_seed(..., scope_kernel_categories, scope_concept_ids)`. Empty inference → no restriction; empty scope filter falls back to full hybrid. Non-LLM fallback only.
- F11.3: `RELATION_NAME_CYPHER` matches `r.normalized_relation` / `r.relation` (existing btree; no new index). Concept vector hits expand `MEMBER_OF` members into the seed list (cap). Existing `concept_embedding` / `relation_embedding` / `relation_fulltext` unchanged.
- F11.4: `POST /graph/query` returns `citations` additively. Subgraph stays stored edges only (`Relation|HAS_CONCEPT|MEMBER_OF`). Derived hops go into the LLM prompt as **Ipotesi S2** and into `citations` with id `{src}|{rel}|{tgt}`. `NodeQueryLog.citations_json`; old logs reconstruct `citations=[]`.
- F11.5: `tests/test_acceptance_query_epistemic_status.py` — asserted-only vs derived cross-domain + planner-before-hybrid + default `citations=[]`.
- `pytest tests/test_acceptance_query_epistemic_status.py tests/test_node_query_engine.py tests/test_acceptance_node_query.py tests/test_node_query_api.py tests/test_node_query_log.py tests/test_models.py tests/test_acceptance_s0_s1_s2.py -q --tb=short`: 67 passed.
- `ruff check` on touched Python files: all checks passed.
- Deviations: no optional LLM planner (token fallback is enough and keeps tests OpenAI-free). Derived edges never enter `subgraph.relationships`. Frontend: additive `QueryCitation` fields on `NodeQueryResponse` only — no Fase 12 panels. `test_scenario8` stub updated for Fase 10 `promoted_parent_ids` / `run_judge`.
- Next = Fase 12. Do not start frontend panels in this phase.

## Fase 12 notes

- UI layer in `frontend/components/*Panel.tsx` + `ConceptDomainExplorer.tsx`: tabbed sidebar (Pipeline / Query / Dominio / Identità / Contraddizioni / Regole / Giudice). Mobile: Pipeline + Query + Layer sheet. Exactly one `EntityEventExplorer` (4 NVL canvases unchanged).
- F12.1: concept overview/neighbors additive `parent_uri` / `kernel_category` / `definition`; overview `IS_A` child→parent; neighbors also `IS_A` + `MEMBER_OF`.
- F12.2: entity graph `has_facets` + `facet_count` when IdentityNode has >1 SAME_AS facet; caption ` · faccette`; `GET/POST /graph/identities` + unlink via `unlink_facet` (no merge/delete Node).
- F12.3–F12.5: `GET /graph/contradictions` (never hidden), `GET /graph/connectivity-rules` (`origin_count`), `GET /graph/judge-runs` newest first.
- F12.6: ASSERITO/DERIVATO badges + expand `derivation_chain`.
- F12.7: vitest on helpers/API paths; pytest FakeSession + TestClient `tests/test_metagraph_layer_api.py`; checklist `frontend/docs/e12-metagraph-ui-checklist.md`.
- Next = Fase 13. Do not start migration/backfill in this phase.

## Fase 13 notes

- F13.1: `backend/scripts/backfill_kernel_category.py` — idempotent batch (`MATCH` where `kernel_category IS NULL AND merged_into IS NULL`), `call_structured` → `EntityKernelType` value (`"Evento"`, not `"E4"`), `--dry-run` / `--limit`. Core loop importable; tests use FakeSession, no live DB.
- F13.2: `DERIVED_FROM` already Famiglia B (`SpecialRelationType.derived_from` / `FAMIGLIA_B_REL_TYPES`); ingestion `CREATE (n)-[:DERIVED_FROM]->(c)` toward `:Chunk`; promote skips it. No Cypher rewrite of existing edges.
- F13.3: flag defaults locked in CI (`ENABLE_KERNEL_CLASSIFICATION`/`PROMOTE`/`TEMPORAL_TRANSITIONS`/`JUDGE` True; `ENABLE_FACET_IDENTITY` False; `ENABLE_DERIVES` False). Existing `backend` unit job covers Metagraph tests (comment only; no Fase 14 E2E job).
- F13.4 deferred: `merge_nodes` still exists and is still called when `ENABLE_FACET_IDENTITY` is false; binary `RelationLabel.replaces`/`extends` kept.
- F13.5: removed seven broken `milestone1/` Documentation bullets; UI checklists point at `frontend/docs/e7-visual-encoding-checklist.md` and `frontend/docs/e12-metagraph-ui-checklist.md`. Zero remaining `milestone1/` paths in README.
- Next = Fase 14. Do not start the Metagraph E2E/CI job in this phase.

## Fase 14 notes

- F14.1: `tests/test_acceptance_metagraph_e2e.py` — fixed mini-corpus constants; real `write_node` / `write_node_relation` / `promote` / `link_as_facet` / `run_judge` / `derive_candidate_links` / `label_query_citations` against in-memory MetaGraphSession (no Docker, embeddings stubbed).
- F14.2: `tests/test_metagraph_schema_integration.py` — Testcontainers `neo4j_gds_container`; Fase 2 constraints/btree; no extra vector indexes; MERGE ConnectivityRule/IdentityNode/CorpusContext/JudgeRun. Ignored by unit `backend` job; run in `backend-integration-metagraph`.
- F14.3: `tests/test_kernel_stress.py` — one test per Doc1 §13 case (facets E1+E7, vertical refinement, EQUIVALENT_TO, cross-domain facts kept, CONTRADICTS completeness, S2 in-memory wall).
- F14.4: CI job `backend-integration-metagraph` (additive; `test_schema.py` still in `backend-integration`). Unit job ignores the new Docker file. Comment “no separate E2E job yet” removed.
- F14.5: extended `frontend/docs/e12-metagraph-ui-checklist.md` (kept name; six §13 manual cases on Fase 12 panels). No Fase 15 macro graph.
- Deviations: E2E orchestrates importable functions + FakeSession rather than `run_ingestion_pipeline` + Docker. `ENABLE_FACET_IDENTITY` still defaults False; tests monkeypatch True locally. `merge_nodes` not removed.
- `pytest tests/test_acceptance_metagraph_e2e.py tests/test_kernel_stress.py tests/test_acceptance_s0_s1_s2.py tests/test_acceptance_judge.py tests/test_readme_links.py -q --tb=short`: 31 passed. `ruff check` on new test files: all checks passed.
- Next = Fase 15. Do not start the macro graph view in this phase.

## Fase 15 notes

- F15.1–F15.2: `GET /graph/macro`, `GET /graph/bundle/{a}/{b}`, `GET /graph/metadata/{id}` in `node_graph_engine.py` + `node_graph.py`. Macro nodes = promoted `:Concept` + leftover `:Node` on kernel/first-level non-promoted homes. Edges collapsed to `BUNDLE` with caption = `relation_count`. Bundle lists stored `:Relation` only (S2 wall intact).
- F15.3–F15.7: `MacroGraphPanel` wraps `GraphPanel` (`fetcher={() => getMacroGraph()}`, `colorByKernelCategory`). `onRelationshipClick` added to GraphPanel. Dashboard toggle dettagliata (default, EntityEventExplorer) ↔ generale (MacroGraphPanel only). Metadata lists, no extra NVL. `IdentityDetailPanel` reused with optional `filterFacetNodeId`.
- F15.8: `tests/test_macro_graph.py` (FakeSession) + vitest `macro-graph-api`, `graph-encoding`, `bundle-detail`. Checklist Fase 15 in `e12-metagraph-ui-checklist.md`.
- Deviations: GraphRelationship not extended with `properties` (count lives in caption + type `BUNDLE`). Derived S2 omitted from bundle (stored edges only). No Fase 16.
- Next phase: none. Plan complete.

## Fase 17 notes

- F17.1 / F17.3 / F17.4 / F17.7: `GET /graph/domains` (lista completa, zero LIMIT), `/domains/{id}/dictionary` (Σ_D tra membri diretti), `/domains/{id}/rules` (ConnectivityRule scoped per kernel_category / concept_id / origin_fact_ids — non `list_connectivity_rules()`), `/domains/{id}/children-graph` (figli IS_A + MEMBER_OF + BUNDLE). Root canvas `GET /graph/domains-graph`: solo `:Concept` con almeno un fascio; i `:Node` foglia sono sollevati via MEMBER_OF e non appaiono. 404 se il Concept manca. `GET /graph/macro` resta.
- F17.2 / F17.5 / F17.6 / F17.8–F17.12: `DomainDashboard` + `DomainDetailCard` + `DomainGraphPanel` (GraphPanel, `colorByKernelCategory`). Stack `drillPath` in `DashboardShell`; freccia Indietro sopra il canvas; lista MEMBER_OF testuale nello scope. `NodeMetadataPanel` riusato con `node_type` entity|event. Un solo NVL: mai EntityEventExplorer e DomainGraphPanel insieme.
- F17.13: `tests/test_domain_dashboard.py` + vitest `domain-dashboard-api.test.ts`; scenari 2 / 2c aggiornati. Checklist Fase 17 in `e12-metagraph-ui-checklist.md`.
- Tests: `pytest tests/test_macro_graph.py tests/test_metagraph_layer_api.py tests/test_acceptance_solo_entita_eventi.py tests/test_domain_dashboard.py -q` → 40 passed. `npx vitest run` → 64 passed (15 files). `ruff check` on touched Python files: all checks passed.
- Deviations: matching rules also on `source_category`/`target_category` == concept id (deposit S1 preferisce il token Concept, non solo `kernel_category`). `MacroGraphPanel.tsx` resta su disco, non montato. Nello scope drill, i figli diretti possono mescolare Concept e Node (F17.7); il vincolo «no :Node sciolti accanto a :Concept» vale sulla radice (`/graph/domains-graph`).
- Next: none in this phase.

## Fase 18 notes

- F18.1: `merge_nodes` after edge move promotes `dup.summary` onto `canon` when `dup.created_at` is newer (previous surface text archived on the fused node). `kernel_category` copied only if canon is missing it. `MERGE_IDENTITY_NODE_CYPHER` overwrites `canonical_summary` on later calls. Judge `_task_identity` picks the more recent facet summary.
- F18.2: `node_history(session, canon_id)` BFS-walks the `merged_into` property chain (any depth) and returns `NodeSnapshot` oldest→newest.
- F18.3: `reconcile_different_tail_pairs` after entity-relation classification in `_run_node_phases`. Same-head / different-tail latest pairs (missed-contradiction shape) scoped to `h.id IN $touched_ids`. Applies `map_temporal_transition` then tail-to-tail `SUPERSEDES`/`UPDATED_BY`/`CONTRADICTS`. Does **not** call `classify_and_apply_entity_relation` (that is same-endpoint). Empty `node_touched` → zero queries.
- F18.4: `FIND_MISSED_CONTRADICTIONS_CYPHER` takes `$touched_ids`; empty/omitted full-scans. `run_dreaming_pipeline` forwards `node_touched` into `run_judge`.
- F18.5: `tests/test_acceptance_graph_update.py` — FakeSession, no Docker/OpenAI.
- No new Settings flag. `ENABLE_FACET_IDENTITY` still default False. `merge_nodes` still exists.
- Tests: `pytest tests/test_acceptance_graph_update.py tests/test_node_resolution.py tests/test_acceptance_identity_facets.py tests/test_acceptance_judge.py tests/test_dreaming_nodes.py tests/test_acceptance_temporal_axis.py -q --tb=short` → 73 passed. Extra regression (`test_acceptance_metagraph_e2e`, `test_kernel_stress`, `test_entity_relation_resolution`, `test_acceptance_migration`, `test_readme_links`) → 33 passed. `ruff check` on touched files: all checks passed.
- Next: none in this phase.

## Fase 19 notes

- F19.1: `write_node` always persists `summary_embedding = embeddings.embed(summary or "")` on CREATE. Name `embedding` path unchanged. New vector index `node_summary_embedding` (768, cosine, `IF NOT EXISTS`) registered in `REQUIRED_VECTOR_INDEXES`.
- F19.2: fulltext `node_summary_fulltext` on `Node.summary`; denormalized `Relation.witness_text` (space-joined `witnesses_a` + `witnesses_b`) + `relation_witness_fulltext`. Schema statements 23 → 26. Both names in `REQUIRED_FULLTEXT_INDEXES`.
- F19.3: `facts_from_source(session, doc_id)` is read-only: chunks by `doc_id` → nodes `[:DERIVED_FROM]` those chunks → incident `:Relation`. No new relation→chunk write path.
- F19.4: `get_node_relations` / `RelationSnapshot` uses new Cypher (not `dup_id`/`canon_id` merge queries).
- F19.5: `backend/app/pipeline/context_retrieval.py` — six isolated wrappers, zero LLM, zero mutating Cypher. `get_metadata` / `get_domain_dictionary` reuse Fase 15/17. `search_vector` embeds arbitrary query text onto `node_summary_embedding`.
- F19.6: `tests/test_context_retrieval.py` — FakeSession unit + Docker (indexes). Embeddings stubbed to 768-dim unit vectors; no OpenAI.
- No new Settings flag. No backfill of old nodes. No agent (Fase 22). Frontend untouched.
- Tests: `pytest tests/test_context_retrieval.py tests/test_schema.py::test_schema_file_has_expected_statements -q --tb=short` → 14 passed. Docker `tests/test_schema.py` + `tests/test_metagraph_schema_integration.py` → 4 passed. `ruff check` on new/touched Python files: all checks passed. `test_acceptance_solo_entita_eventi.py` schema union updated; 4 pre-existing frontend failures (DashboardShell / missing EntityEventExplorer) are unrelated to this phase.
- Suggested commit: `feat: add metadata-rich retrieval for the agentic context layer`
- Next = Fase 20. Do not start relevance gate / PendingHypothesis in this phase.

## Fase 20 notes

- F20.1: `backend/app/pipeline/relevance_gate.py` — pure `classify_fragment_relevance` (T1 predicate-without-pair, T2 explicit markers, T3 S0-without-comparables + T2). Zero LLM. `ERROR_MARKERS` / `SUCCESSION_MARKERS` exported from `entity_relation_resolution.py`; new `QUANTIFIER_MARKERS` / `RETRACTION_MARKERS` live in the gate.
- F20.2: ingest hook after Pass B in `process_chunk_node_extraction` (also when `pair_jobs` is empty), behind `ENABLE_CONTEXT_LAYER`. Signal → `route_chunk_signal`; `None` → no hypothesis. T3 comparables = `FIND_DIFFERENT_TAIL_PAIRS_CYPHER` scoped to the chunk's node ids (F18.3 pairing shape, no LLM).
- F20.3: additive `CREATE CONSTRAINT pending_hypothesis_id` (schema statements 26 → 27). `pending_hypothesis.py`: `create_or_reinforce_hypothesis` / `list_open_hypotheses` / `resolve_hypothesis`. Deterministic id = sha256(`pending:` + normalized claim_target). Same claim_target MERGEs; a new span reinforces, an identical span is a no-op.
- F20.4: single fragment without named witnesses → `confidence=low`, `status=open`, no event. Named witness → `high` + `event_bus.publish(..., "context_layer", "hypothesis_promoted", ...)`. Second fragment → `medium` + publish. Status stays `open` (`promoted=true`); no S0 write. Settings: `ENABLE_CONTEXT_LAYER=False`, `PENDING_HYPOTHESIS_LISTEN_WINDOW=5`.
- F20.5: `listen_open_hypotheses` after chunk processing in `run_ingestion_pipeline`. Re-reads `open` hyps via F19 `facts_from_source` / `search_fulltext` / `search_vector` plus lexical overlap. Same-`doc_id` origin is skipped (listen is for subsequent documents). Contradiction → `dismissed`. No overlap after the window → stays `open`, no auto-promotion.
- F20.6: `tests/test_acceptance_relevance_gate.py` (12) + `tests/test_pending_hypothesis.py` (12), FakeSession, no Docker.
- Tests: `pytest tests/test_acceptance_relevance_gate.py tests/test_pending_hypothesis.py tests/test_schema.py::test_schema_file_has_expected_statements tests/test_acceptance_ingest_antiblur.py tests/test_chunking.py -q --tb=short` → 39 passed. `ruff check` on new/touched Python files: all checks passed.
- Deviations: T3 at ingest approximates F18.3 with the existing different-tail query (dreaming-time reconcile is unchanged). Promotion does not set `status=confirmed` — Fase 22 consumes the event. Quantifier events / retractions / context_agent not started.
- Suggested commit: `feat: add structural relevance gate and pending hypotheses`
- Next = Fase 21. Do not start quantifier events / retractions / context_agent in this phase.

## Fase 21 notes

- F21.1: `backend/app/pipeline/quantifier_events.py` — `resolve_quantifier_scope`. T2 quantifier climbs `MEMBER_OF`/`IS_A` to the named genre, collects in-scope leaf members starting from this `doc_id` (`DERIVED_FROM` → `:Chunk`), plus named witnesses and same-place `Spaziale` tails of this document — never the full genre extension. **Closed scope** = ≥2 identified leaf members sharing the scene (same document is the minimum signal). Closed → `reify_shared_situation` (Evento + R5 `participates`) and `SUPERSEDES`/`UPDATED_BY` on prior state/place via `map_temporal_transition` + `_apply_different_tail_temporal`. Not closed → `create_or_reinforce_hypothesis` (low/open), zero S0. Never a collective entity.
- F21.2: `backend/app/pipeline/retraction.py` — `resolve_retraction_scope`. Identifiable source (this-document deictic or a named `doc_id`) → `facts_from_source` then Famiglia B (`write_contradicts`, or `UPDATED_BY` when error markers fire) only on that source's relation-facts. Unidentifiable → hypothesis `evidence_gap="fonte non identificata"`, zero fan-out, no `facts_from_source`. Never `DELETE`.
- F21.3: retraction module never matches `:CorpusContext` / `update_corpus_context`; regression asserts Cypher calls and source text.
- F21.4: `tests/test_acceptance_quantifier_events.py` (6) + `tests/test_acceptance_retraction.py` (7), FakeSession, no Docker. Ingest hooks `maybe_resolve_*` behind `ENABLE_CONTEXT_LAYER` after F20.2 in `process_chunk_node_extraction`. No new flag. No `context_agent.py`.
- Tests: `pytest tests/test_acceptance_quantifier_events.py tests/test_acceptance_retraction.py tests/test_acceptance_relevance_gate.py tests/test_pending_hypothesis.py tests/test_acceptance_ingest_antiblur.py tests/test_event_relation_resolution.py -q --tb=short` → 57 passed. `ruff check` on new/touched Python files: all checks passed.
- Deviations: (1) quantifier state updates coerce `map_temporal_transition` `none`/`extends` to `SUPERSEDES` (leaving a scene is succession, not token overlap); (2) retractions ignore T1 succession markers because Italian «finora è» contains the substring `ora è` — default `CONTRADICTS` unless error wording → `UPDATED_BY`; (3) R5 participates go through Fase 6 `MERGE_SITUATION_PARTICIPATES_CYPHER` (no witnesses on that MERGE; `write_node_relation` is used for the new state/place fact). Fase 22 not started.
- Suggested commit: `feat: add quantifier events and source-scoped retractions`
- Next = Fase 22. Do not start `context_agent.py` / `:AgentSearchRun` in this phase.

## Notes

- After each phase: verify acceptance criteria, commit, push to origin, update README if needed.
- Do not modify files outside `sm/`.
