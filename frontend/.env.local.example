NEXT_PUBLIC_API_URL=http://localhost:8000
# true = offline mock pipeline (E8); false = live SSE with job_id in the UI
NEXT_PUBLIC_USE_MOCK_EVENTS=false
# true = Query Panel uses local fixture (E9.1); false = real POST /query (E9.3 default)
NEXT_PUBLIC_USE_QUERY_FIXTURE=false
