"use client";

/**
 * Panel wrapper used by the dashboard shell (E6/E7).
 */

import { GraphExplorer } from "@/components/GraphExplorer";

export function GraphExplorerPanel() {
  return <GraphExplorer />;
}
