# Macrotask 9 loop progress — giudice eventi: ricerca attiva

Source plan: `../macrotask-9-giudice-eventi-ricerca-attiva.md`
Theory refs: `../1.metagraph-formalizzazione-rev.md`, `../2.metagraph-kernel-tre-assi (2).md`, `../3.metagraph-pipeline-formalizzazione (1).md`, `../4.metagraph-specifiche-aperte-baseline.md`, `../5.metagraph-layer-agentico-contesto.md`
Prior plan (MT 0–8, already done): `../implementation_plans_done/piano-implementativo-giudice-eventi-assert-retract.md`
Allowed write root: `sm/` only.
Git remote: `https://github.com/aantonioderosaa/meta-graph-sm.git` (repo is `sm/`).
Loop sentinel: `AGENT_LOOP_WAKE_mt9react`

## Current

- **Completed phase:** 1 — Fix §5 (bug #4: `apply_validated_slot` returns True after a silent no-op write)
- **In progress:** none
- **Next:** 2 — `verified_no_change` + `_record_verdict`
- **Status:** Phase 1 AC met. Pending Phase 2. Do **not** skip ahead to the ReAct loop before phases 1–3 are done.
- Keep `ENABLE_EVENT_TRIAGE` default False.

## Why this order

Bug #4 is worse than “the agent discards too much”: an LLM-hallucinated `head_id`/`tail_id` currently yields `confirmed` without writing anything in the graph. Adding search capacity on top of self-deception would make things worse.

## Phases (§9)

| # | Nome | Status |
|---|---|---|
| 1 | Fix §5 rows-affected (`apply_validated_slot` must return False when MATCH writes 0 rows) | completed |
| 2 | Schema §6 `verified_no_change` + `_record_verdict` | pending |
| 3 | Verifica §4 contesto grezzo (chunk text via `DERIVED_FROM`) | pending |
| 4 | Loop ReAct §2–3 (`EVENT_TRIAGE_MAX_TURNS=3`, search before giving up, never invent ids, judge never creates `:Node`) | pending |
| 5 | Test a corpus fisso §8 (tre casi di riferimento, incluso evento senza entità pre-collegata) | pending |

## Phase 1 acceptance (must all pass before commit/push)

- `apply_validated_slot` returns `False` when `head_id` or `tail_id` is not an existing `:Node` (hallucinated id). Zero `:Relation` created. **Met.**
- `apply_validated_slot` returns `False` when `retract_slot` is a true no-op (fonte not in the supporting set). **Met.**
- `apply_validated_slot` still returns `True` on a real assert/retract that changed the graph (or an idempotent re-assert of an already-supporting fonte on an existing slot whose endpoints exist). **Met.**
- Injecting an invented id in a test: verdict of that event is **not** `confirmed` (`applied` stays 0). **Met.**
- Existing MT0–8 tests still pass. No behavior change outside `event_slots.py` / tests except whatever is required so `_apply_proposal` already counting `wrote` stays honest. **Met** (`event_triage.py` production code unchanged).
- `ENABLE_EVENT_TRIAGE` stays False. No ReAct loop yet. No `verified_no_change` yet. **Met.**

## Phase 1 implementation notes

- Detection: `MATCH_BOTH_NODES_CYPHER` (`MATCH (h:Node {id:$head_id}), (t:Node {id:$tail_id}) RETURN …`). Empty result → `assert_slot` returns False **before** `write_node_relation` / stamp / deposit.
- `assert_slot` / `retract_slot` now return `bool`. `apply_validated_slot` propagates them (never `True` after a silent miss).
- Retract: true no-op (fonte absent or unknown slot) → False; at least one `_update_slot_edge` → True.
- FakeSession CREATE/STAMP/FIND/UPDATE now refuse missing endpoint nodes (Neo4j MATCH-both semantics).
- Phases 2–5 not started.

### Files touched

- `sm/backend/app/pipeline/event_slots.py`
- `sm/backend/tests/test_event_slots.py`
- `sm/backend/tests/test_validate_slot_proposal.py`
- `sm/backend/tests/test_event_triage.py`
- `sm/.macrotask-9-loop-progress.md`

### Tests run

From `sm/backend` with `.venv\Scripts\python.exe`:

```
pytest tests/test_event_slots.py tests/test_validate_slot_proposal.py tests/test_event_triage.py tests/test_acceptance_event_triage.py tests/test_node_revisions.py tests/test_reconcile_attribute_slots.py -q --tb=short
ruff check app/pipeline/event_slots.py app/pipeline/event_triage.py tests/test_event_slots.py tests/test_validate_slot_proposal.py tests/test_event_triage.py
```

- pytest: **95 passed**
- ruff: **clean** (one import-sort auto-fix)

### Deviations

None. `_apply_proposal` already did `if wrote: applied += 1`; production `event_triage.py` was not modified.

### Leftover gaps

None for Phase 1. Phase 2 (`verified_no_change`) is next.

## Notes

- After each phase: verify acceptance criteria, commit, push to origin, update README if needed.
- Do not modify files outside `sm/`.
- The judge never creates new `:Node` — assert/retract only on existing nodes. Render that explicit in code when phase 4 lands; phase 1 already refuses hallucinated ids.
- Never DELETE, never merge_nodes, never touch identity.
- Kernel Famiglia B stays closed.
