name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/pyproject.toml
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Ruff
        run: ruff check .
      - name: Unit tests (no Docker)
        # Metagraph unit tests (Fasi 0–14 E2E + kernel stress) ride this job.
        # Docker schema/GDS tests: backend-integration + backend-integration-metagraph.
        run: >
          pytest -q
          --ignore=tests/test_schema.py
          --ignore=tests/test_health.py
          --ignore=tests/test_ingestion_integration.py
          --ignore=tests/test_embeddings.py
          --ignore=tests/test_nodes_integration.py
          --ignore=tests/test_ppr_projection_integration.py
          --ignore=tests/test_node_query_engine_integration.py
          --ignore=tests/test_documents_list.py
          --ignore=tests/test_graph_reset.py
          --ignore=tests/test_metagraph_schema_integration.py

  backend-integration:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/pyproject.toml
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Acceptance + integration (Docker / testcontainers)
        run: >
          pytest -q
          tests/test_schema.py
          tests/test_health.py
          tests/test_ingestion_integration.py
          tests/test_nodes_integration.py
          tests/test_ppr_projection_integration.py
          tests/test_node_query_engine_integration.py
          tests/test_documents_list.py
          tests/test_graph_reset.py
          --tb=short

  backend-integration-metagraph:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/pyproject.toml
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Metagraph schema integration (Docker / testcontainers)
        run: >
          pytest -q
          tests/test_metagraph_schema_integration.py
          tests/test_schema.py
          --tb=short

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install dependencies
        run: npm ci --legacy-peer-deps
      - name: Lint
        run: npm run lint
      - name: Unit tests
        run: npm test
      - name: Build
        run: npm run build
